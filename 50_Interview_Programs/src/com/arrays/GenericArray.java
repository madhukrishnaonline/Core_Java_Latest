package com.arrays;

public class GenericArray {
	/*public static <T> T[] addValues(Class<T> clazz, int size, T... values) {
		int len = values.length;
		if (size < len) {
			throw new IllegalArgumentException("Enter the correct size of values");
		}
		T arr[] = (T[]) Array.newInstance(clazz, size);
	
		int index = 0;
		for (T i : values) {
			arr[index] = i;
			index++;
		} // for
		return arr;
	}*/

	public static void main(String[] args) {
		/*Scanner scanner = new Scanner(System.in);
		System.out.println("Enter the Size");
		int size = scanner.nextInt();
		//	String[] arr = addValues(String.class, size, "madhu", "krishna", "kummari");
		
		for (String i : arr) {
			System.out.println(i);
		} // for
		
		scanner.close();*/
	}// main

}
